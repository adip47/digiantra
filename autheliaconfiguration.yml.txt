host: 0.0.0.0
port: 9091
log_level: debug

authentication_backend:
  file:
    path: /config/users_database.yml

session:
  name: authelia_session
  secret: somerandom_secret
  expiration: 1h
  inactivity: 5m
  domain: localhost

access_control:
  default_policy: deny
  rules:
    - domain: localhost
      policy: one_factor

regulation:
  max_retries: 3
  find_time: 2m
  ban_time: 5m

storage:
  local:
    path: /config/db.sqlite3

notifier:
  filesystem:
    filename: /config/notification.txt
