server:
  host: 0.0.0.0
  port: 9091

log:
  level: info

jwt_secret: a_super_secret_jwt_key

default_redirection_url: https://localhost

session:
  name: authelia_session
  secret: another_super_secret_session_key
  expiration: 3600
  inactivity: 300
  domain: localhost

authentication_backend:
  file:
    path: /config/users_database.yml

access_control:
  default_policy: deny
  rules:
    - domain: "localhost"
      policy: one_factor

storage:
  local:
    path: /config/db.sqlite3

notifier:
  filesystem:
    filename: /config/notification.txt
